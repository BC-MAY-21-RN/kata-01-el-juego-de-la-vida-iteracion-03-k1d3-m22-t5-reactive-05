const Celula = require('./celula');


describe("Pruebas de la clase Cell", () => {
  test("probamos el estado random", () => { });
  test("probamos el estado random", () => { });
  test("probamos el estado random", () => { });
  test("probamos el estado random", () => { });
});
